{
  "buildCommand": "if [ -d \"frontend\" ]; then cd frontend && npx expo export -p web; else npx expo export -p web; fi",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
